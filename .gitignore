# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Env files — .env já estava versionado (só com a chave pública, que é segura
# por design), mas deixar aberto é uma armadilha: bastaria alguém colar a
# SUPABASE_SERVICE_ROLE_KEY ali por engano pra vazar de verdade. Segredo real
# só em .env.local (coberto por *.local acima).
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
